SELECT u.id, u.name, r.name, c.name, e."startDate" FROM users u JOIN experiences e ON u.id = e."userId" JOIN roles r ON e."roleId"=r.id JOIN companies c ON e."companyId" = c.id WHERE u.id=50 AND e."endDate" IS NOT NULL;
